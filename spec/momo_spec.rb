# frozen_string_literal: true

RSpec.describe Momo do
  it 'has a version number' do
    expect(Momo::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
